You are an expert Linux DevOps engineer and Node.js deployment engineer.

I have an Oracle Cloud Ubuntu 22.04 ARM VM deployment script (setup.sh) for my WhatsApp bot.

There is one deployment bug that must be permanently fixed.

Current problem:

The script successfully runs:

- git clone
- npm install
- npm install exits successfully
- package.json already contains:
  "fs-extra": "^11.4.0"

After deployment, running

node -e "require('fs-extra'); console.log('OK')"

prints:

OK

and

npm ls fs-extra

shows fs-extra installed correctly.

However setup.sh still stops with:

❌ fs-extra clean reinstall ke baad bhi missing hai

This is a FALSE POSITIVE.

The deployment must NEVER fail when fs-extra is already installed.

Your task:

1. Find the dependency verification code.

2. Remove every filesystem check such as:

[ -f node_modules/fs-extra/index.js ]

or any similar file existence validation.

Never validate packages by checking files.

3. Replace the verification with proper Node.js module resolution:

node -e "require.resolve('fs-extra')"

or

node -e "require('fs-extra')"

4. If npm install exits with code 0, deployment must continue.

Never stop deployment only because fs-extra verification failed.

5. If fs-extra cannot be resolved:

- run

npm install fs-extra --save

once.

6. After that, verify again using require.resolve().

7. If it still cannot be resolved, print a WARNING only.

Do NOT exit setup.sh.

Deployment must continue.

8. Remove every line like:

fail "fs-extra clean reinstall ke baad bhi missing hai"

or

exit 1

or

return 1

related to fs-extra.

9. Keep all real npm failures intact.

If npm install itself fails, deployment should stop normally.

10. Do not modify any other deployment logic.

Keep MongoDB setup, .env generation, PM2, Nginx, Certbot, Firewall, Oracle tuning exactly as they are.

11. Make the dependency verification generic.

Create a reusable function such as:

verify_dependency() {
    local pkg="$1"

    if node -e "require.resolve('$pkg')" >/dev/null 2>&1; then
        return 0
    fi

    npm install "$pkg" --save

    node -e "require.resolve('$pkg')" >/dev/null 2>&1 && return 0

    echo "WARNING: $pkg could not be verified."
    return 0
}

Then call:

verify_dependency fs-extra

This function must NEVER abort deployment.

12. Also remove every --silent flag from npm install so deployment logs remain visible.

13. Ensure setup.sh finishes successfully if npm install completed successfully.

Output only the modified code blocks with comments explaining what changed.