✔  Node.js packages installed
✔  Dependency verified: fs-extra
✔  Dependency verified: axios
✔  Dependency verified: @whiskeysockets/baileys

━━━  12. Bot Directories  ━━━ (15:19:55)
✔  Bot directories ready: logs temp session downloads database cache

━━━  13. Public IP + Domain  ━━━ (15:19:55)
▶  Public IP: 130.110.123.57
▶  Domain: 130-110-123-57.nip.io (nip.io — no DNS config needed)
✔  IP=130.110.123.57  DOMAIN=130-110-123-57.nip.io

━━━  14. .env Configuration  ━━━ (15:19:56)
✔  .env already exists — merging missing variables only
▶  .env: PORT already set — skip kiya
▶  .env: SERVER_ID already set — skip kiya
▶  .env: MONGODB_URI already set — skip kiya
▶  .env: SESSION_SECRET already set — skip kiya

  ━━━ Optional: Telegram Bot Tokens ━━━
  (30 saniye mein Enter dabao skip karne ke liye)

  TELEGRAM_BOT_TOKEN (Enter to skip):
✔  .env configuration complete

━━━  15. PM2 Ecosystem Config  ━━━ (15:20:26)
▶  ecosystem.config.cjs fresh likh rahe hain (dotenv explicit load — PM2 v7 env_file workaround)...
✔  ecosystem.config.cjs written (50 lines)

━━━  16. Oracle Cloud iptables Fix  ━━━ (15:20:26)
✔  iptables: port 22 (tcp) ACCEPT appended
✔  iptables: port 80 (tcp) ACCEPT appended
✔  iptables: port 443 (tcp) ACCEPT appended
✔  iptables: port 5000 (tcp) ACCEPT appended
▶  iptables rules save ho rahi hain (netfilter-persistent)...
✔  iptables rules permanently saved

━━━  17. UFW Firewall  ━━━ (15:20:26)
✔  UFW: SSH(22) HTTP(80) HTTPS(443) Dashboard(5000) open | MongoDB(27017) blocked

━━━  18. Nginx Configuration  ━━━ (15:20:27)
tee: /etc/nginx/sites-available/130-110-123-57.nip.io: No such file or directory

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌  Unexpected error at line 0 (exit code: 1)
    Last command: sudo tee "$NGINX_CONF" > /dev/null <<NGINXCONF
# AA MD Bot — Nginx Config for ${DOMAIN}
# Reverse proxy to Node.js dashboard on port 5000
# HTTPS/SSL added by certbot automatically

server {
    listen 80;
    listen [::]:80;
    server_name ${DOMAIN};

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Proxy to bot dashboard
    location / {
        proxy_pass         http://127.0.0.1:5000;
        proxy_http_version 1.1;

        # WebSocket support (needed for real-time dashboard)
        proxy_set_header Upgrade    \$http_upgrade;
        proxy_set_header Connection 'upgrade';

        proxy_set_header Host             \$host;
        proxy_set_header X-Real-IP        \$remote_addr;
        proxy_set_header X-Forwarded-For  \$proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto \$scheme;

        proxy_cache_bypass \$http_upgrade;

        # Timeouts
        proxy_connect_timeout  60s;
        proxy_send_timeout     60s;
        proxy_read_timeout     60s;
    }

    # Large file uploads (media sharing)
    client_max_body_size 200M;
}
NGINXCONF

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
