# Public npm registry — ensures npm install works on Oracle VM, Railway, Docker.
# NOTE: On Replit, this file is ignored (Replit forces its own internal proxy).
#       The deploy/setup.sh script uses --registry flag explicitly for Oracle VM.
registry=https://registry.npmjs.org/
